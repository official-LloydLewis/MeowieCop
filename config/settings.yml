bot:
  name: MoewieCop
  version: "0.1.0"
  api_timeout: 10

storage:
  database_path: "storage/database.yml"

moderation:
  rate_limit_per_second: 20
  default_mute_duration: "10min"

commands:
  enabled:
    - mute
    - unmute
    - ban
    - unban
    - info
    - admin
